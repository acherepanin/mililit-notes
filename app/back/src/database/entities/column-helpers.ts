/**
 * Значение по умолчанию для текстовых колонок `created_at` / `updated_at`.
 * Postgres-выражение возвращает UTC-время как ISO-подобную строку, чтобы
 * сервисы и мапперы повсюду работали с таймстампами как со строками.
 */
export const NOW_TEXT = "(now() AT TIME ZONE 'utc')::text";
